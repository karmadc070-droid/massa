# massa 토스페이먼츠 연동 계획 (2026-08-29)

## 무엇을 왜 만드는가

지금 massa 는 결제가 전부 **현장 후불**(카드·QR·현금)이다. 여기에 **선불 카드 결제**를 선택지로 더한다.
목적은 PRD 에 적힌 노쇼 방지다. 후불을 없애지는 않는다 — 심사 중인 1.0.2 의 설명·안내문을 건드리지 않아도 되고,
현지 고객은 계속 현장 결제를 쓸 수 있다.

## 확인한 사실

| 항목 | 결론 | 근거 |
|---|---|---|
| Apple 심사 | 문제없음. 오히려 외부 결제를 **써야 한다** | 가이드라인 3.1.3(d) 실시간 대인 서비스, 3.1.3(e) 앱 밖에서 소비되는 서비스 |
| 토스 계약 | 가능 (한국 사업자) | 토스페이먼츠는 국내 사업자만 계약 가능, 해외 법인 불가 |
| 통화 | 원화로 청구 | 토스 지원 통화는 KRW·USD·JPY. VND 는 미지원. 앱에 이미 `×0.054` 환산 코드가 있다 |
| 서버 | 새로 만들 필요 없음 | `massa-edge-functions` 컨테이너 가동 중, 함수 4개 배포 중 |
| DB | 소폭 변경 | `payment_method` enum + `is_paid` 컬럼이 이미 있다. 예약 7건(테스트)이라 부담 없음 |

### 모아학원에서 가져올 수 있는 것 — 거의 없다

`결제-도입-계획.md` 에는 "Edge Function·테이블이 이미 되어 있다"고 적혀 있지만 실제로는 다르다.

- `toss-payment` 함수는 **소스에도 서버에도 없다** (`/root/moa/volumes/functions` 에 hello·main 뿐)
- `payment_transactions` 테이블 정의도 없다
- 토스 키 미발급, 실제 거래 0건
- 클라이언트 `tossPay()` / `handleTossRedirect()` 만 작성돼 있다 — 이 **흐름은 참고할 만하다**

그리고 모아학원은 **iOS 에서 결제를 꺼뒀다**.

```js
const PAY_ENABLED_ON_IOS = false;   // 돌아올 successUrl 을 만들 수 없다
```

massa 는 iOS 가 주력이라 이 결정을 따라가면 안 된다. 아래 딥링크 방식으로 푼다.

## iOS 결제창 복귀 설계

문제: Capacitor 웹뷰는 `capacitor://localhost` 에서 뜬다. 토스에 넘길 `successUrl` 은 https 여야 하는데,
https 로 돌아오면 앱이 아니라 브라우저다.

풀이: **중계 페이지 + 앱 URL 스킴**

```
앱 → Browser.open(토스 결제 페이지)        ← SFSafariViewController (Apple 허용)
      ↓ 결제 인증
토스 → https://massa.moahagwon.com/pay-return.html?...   ← successUrl
      ↓ 이 페이지가 스킴으로 되던짐
      → massa://pay?orderId=..&paymentKey=..&amount=..
      ↓ Capacitor App.appUrlOpen 수신
앱 → Edge Function confirm → 승인 완료
```

웹·안드로이드에서는 중계 페이지가 그 자리에서 승인까지 처리하고 끝낸다(스킴으로 안 던진다).

## 금액 위변조 방지

클라이언트가 보낸 금액을 **믿지 않는다**.

- `create` 는 `booking_id` 만 받고, 금액은 서버가 `bookings.amount_vnd` 에서 읽어 환산한다
- `confirm` 은 토스가 돌려준 금액과 `payment_transactions.amount` 를 대조한 뒤에만 승인한다
- 시크릿 키는 Edge Function 환경변수에만 둔다. 앱 번들에 절대 넣지 않는다

## 단계

- **P1** DB — `payment_transactions` 테이블, `payment_method` enum 에 `toss`, RLS
- **P2** Edge Function `toss-payment` — create / confirm / cancel
- **P3** 웹 — `pay-return.html` 중계 페이지, 웹 결제 흐름
- **P4** iOS — URL 스킴 등록, `@capacitor/browser`, `appUrlOpen` 복귀 처리
- **P5** 앱 UI — 결제 시트에 "카드로 미리 결제", 예약 취소 시 환불 연결
- **P6** 테스트 키로 전 구간 검증 → 라이브 키 교체

## 키가 없는 동안

`TOSS_SECRET_KEY` 가 비어 있으면 **테스트 모드**로 돈다 — 토스 API 를 부르지 않고 승인된 것처럼 기록한다.
키가 들어오면 환경변수만 채우고 컨테이너를 다시 올리면 실결제로 전환된다. 코드는 그대로다.

## 사장님이 해야 하는 것

1. 토스페이먼츠 가입 (사업자등록증 제출). **가입 직후 테스트 키가 바로 나온다** — 심사를 기다릴 필요 없다
2. 상점 등록 후 `TOSS_CLIENT_KEY` / `TOSS_SECRET_KEY` 를 VPS 에 입력 (값은 내가 다루지 않는다)

## 짚어둘 것 (법률 자문 아님)

- 선불을 받으면 **환불 정책**이 필요하다. 지금 취소 규정은 "예약 1시간 전까지 무료 취소"인데,
  선불 건은 이때 실제 환불이 나가야 한다. 약관에 환불 주체·기한을 명시할 것
- massa 계좌로 받아 나중에 테라피스트에게 정산하면 타인 자금을 보관하는 구조가 된다.
  전자금융거래법 등록 문제가 생길 수 있으니 토스 담당자와 확인할 것 (모아학원도 같은 이유로 서브몰을 택했다)
