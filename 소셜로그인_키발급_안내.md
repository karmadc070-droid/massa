# 소셜 로그인 키 발급 안내 (구글 · 카카오 · 애플)

앱·서버 코드는 다 준비돼 있습니다. **키만 넣으면 버튼이 자동으로 나타납니다.**
키가 없는 동안에는 버튼이 아예 안 보이므로, 지금 상태로 심사에 올려도 안전합니다.

> 왜 이렇게 만들었나 — 예전에 "구글 버튼은 있는데 눌러도 안 됨"으로 Apple 4.0 리젝을 받았습니다.
> 앱이 서버에 "어떤 로그인이 켜져 있나"를 물어보고, 켜진 것만 그립니다.

공통으로 쓰는 콜백 주소는 하나입니다.

```
https://api.moahagwon.com/auth/v1/callback
```

---

## 1. 구글

1. https://console.cloud.google.com → 프로젝트 선택(없으면 생성)
2. **API 및 서비스 → OAuth 동의 화면**
   - User Type: 외부
   - 앱 이름 `massa`, 지원 이메일, 개발자 연락처 입력
   - 게시 상태를 **프로덕션**으로 (테스트 상태면 등록한 계정만 로그인됩니다)
3. **사용자 인증 정보 → 사용자 인증 정보 만들기 → OAuth 클라이언트 ID**
   - 애플리케이션 유형: **웹 애플리케이션**
   - 승인된 리디렉션 URI에 위 콜백 주소 입력
4. 나온 **클라이언트 ID**와 **클라이언트 보안 비밀번호**를 받아 둡니다

---

## 2. 카카오

1. https://developers.kakao.com → 내 애플리케이션 → **애플리케이션 추가하기**
2. **앱 설정 → 플랫폼 → Web** 에 사이트 도메인 등록
   ```
   https://massa.moahagwon.com
   ```
3. **제품 설정 → 카카오 로그인**
   - 활성화 **ON**
   - Redirect URI에 위 콜백 주소 등록
4. **제품 설정 → 카카오 로그인 → 보안** 에서 **Client Secret** 생성 후 활성화 ON
5. **동의항목** 에서 최소한 아래를 설정
   - 닉네임: 필수 동의
   - 카카오계정(이메일): 필수 동의 *(이메일이 없으면 계정 연결이 어렵습니다)*
6. **앱 키 → REST API 키**(= 클라이언트 ID)와 **Client Secret** 을 받아 둡니다

---

## 3. 애플 — 가장 손이 많이 갑니다

애플은 **소셜 로그인이 하나라도 있으면 반드시 함께 있어야 합니다**(가이드라인 4.8).
구글·카카오를 켜면서 애플을 안 켜면 리젝됩니다.

1. https://developer.apple.com/account → **Certificates, Identifiers & Profiles**
2. **Identifiers → app.massa.hanoi** 선택 → **Sign In with Apple** 체크 → 저장
3. **Identifiers → + → Services IDs** 로 새로 생성
   - Description: `massa web auth`
   - Identifier: `app.massa.hanoi.web` *(이게 클라이언트 ID가 됩니다)*
   - 생성 후 다시 열어 **Sign In with Apple → Configure**
     - Primary App ID: `app.massa.hanoi`
     - Domains: `api.moahagwon.com`
     - Return URLs: 위 콜백 주소
4. **Keys → + →** 이름 입력 후 **Sign in with Apple** 체크 → Configure에서 Primary App ID 선택
   - 생성하면 **.p8 파일이 딱 한 번만 내려받아집니다.** 잃어버리면 다시 만들어야 합니다
   - **Key ID** 와 **Team ID**(`GRF3HK77HU`)도 함께 적어 둡니다
5. 이 .p8 로 **클라이언트 시크릿(JWT)** 을 만들어야 합니다 — 이건 제가 해드립니다.
   `.p8` 파일과 Key ID만 알려주시면 됩니다 *(파일 내용은 제가 열지 않고 서버에서 바로 처리합니다)*

> 애플 시크릿은 **최대 6개월**짜리입니다. 만료 전에 다시 만들어야 로그인이 끊기지 않습니다.
> 만료일을 달력에 적어 두시는 걸 권합니다.

---

## 키를 받은 뒤

VPS `/root/massa/.env` 에 아래 값을 채우고 auth 컨테이너를 다시 올리면 끝입니다.
제가 스크립트로 처리하겠습니다 — **값은 제가 보지 않는 방식으로 넣습니다.**

```
GOOGLE_ENABLED=true
GOOGLE_CLIENT_ID=...
GOOGLE_SECRET=...

KAKAO_ENABLED=true
KAKAO_CLIENT_ID=...        # REST API 키
KAKAO_SECRET=...           # Client Secret

APPLE_ENABLED=true
APPLE_CLIENT_ID=app.massa.hanoi.web
APPLE_SECRET=...           # .p8 로 만든 JWT
```

## 순서 제안

구글·카카오는 30분이면 끝납니다. 애플이 오래 걸리니 **구글·카카오를 먼저 켜서 웹에서 검증**하고,
애플이 준비되면 **셋을 함께 켠 채로 iOS 빌드를 올리는** 편이 안전합니다.

⚠️ **애플 없이 구글·카카오만 켠 상태로 iOS 빌드를 제출하면 4.8로 리젝됩니다.**
웹(massa.moahagwon.com)에서 먼저 쓰는 건 상관없습니다.
