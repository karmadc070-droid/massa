# mㅏssㅏ 소셜 로그인(Google·Kakao) 설정 가이드

와이어프레임의 로그인 창에는 이미 "Google로 계속", "카카오로 계속" 버튼이 있고 `signInWithOAuth`로 연결돼 있습니다. 아래 설정을 마치면 버튼이 실제로 동작합니다.

프로젝트 정보
- 슈퍼베이스 프로젝트: massa
- 프로젝트 ref: `jfnpgjcesywsxxbwlpii`
- Auth 콜백 URL(양쪽 공급자에 공통으로 등록): `https://jfnpgjcesywsxxbwlpii.supabase.co/auth/v1/callback`

## 0. 사전 주의 — 슈퍼베이스 로그인 계정
massa 프로젝트는 `karmadc Org`(karmadc070) 계정에 있습니다. 브라우저에서 슈퍼베이스 대시보드를 열 때 이 계정으로 로그인해야 massa가 보입니다. 다른 계정(파크디씨스 조직)으로는 massa 설정에 접근할 수 없습니다.

또한 OAuth는 `file://`로 연 로컬 HTML에서는 리다이렉트가 동작하지 않습니다. 테스트하려면 프로토타입을 로컬 웹서버나 호스팅(예: `python -m http.server`, Netlify 등)으로 http(s) 주소에서 열어야 합니다.

## 1. Google 로그인

1. Google Cloud Console(console.cloud.google.com) → 프로젝트 생성/선택.
2. APIs & Services → OAuth consent screen 구성(외부, 앱 이름 mㅏssㅏ, 지원 이메일 입력).
3. APIs & Services → Credentials → Create Credentials → OAuth client ID → Application type: Web application.
4. Authorized redirect URIs에 추가: `https://jfnpgjcesywsxxbwlpii.supabase.co/auth/v1/callback`
5. 생성되면 Client ID와 Client Secret 복사.
6. 슈퍼베이스 대시보드 → Authentication → Providers → Google → Enable 켜기 → Client ID / Secret 붙여넣기 → Save.

## 2. Kakao 로그인

1. Kakao Developers(developers.kakao.com) → 내 애플리케이션 → 애플리케이션 추가하기.
2. 앱 설정 → 요약 정보에서 REST API 키 확인(= 슈퍼베이스의 Client ID로 사용).
3. 카카오 로그인 → 활성화 ON.
4. 카카오 로그인 → Redirect URI 등록: `https://jfnpgjcesywsxxbwlpii.supabase.co/auth/v1/callback`
5. 보안 → Client Secret 생성 후 사용 설정(= 슈퍼베이스의 Client Secret).
6. 동의 항목에서 필요한 정보(닉네임·이메일 등) 사용 설정. 이메일은 검수/추가 동의가 필요할 수 있음.
7. 슈퍼베이스 대시보드 → Authentication → Providers → Kakao → Enable → Client ID(REST API 키) / Secret 붙여넣기 → Save.

## 2-1. 이메일 로그인 (지금 바로 동작)

소셜 로그인 설정 전에도 이메일 로그인은 즉시 됩니다. 로그인 창에 데모 계정(demo@massa.app / massa1234)이 입력돼 있으니 "회원가입 / 로그인"을 누르면 로그인됩니다. 만약 로컬 파일(file://)이 아니라 http 주소에서 열어야 안정적으로 동작합니다.

로그인이 안 될 때 점검 순서
1. massa 프로젝트가 활성 상태인지(무료 플랜은 자동 일시중단될 수 있음) — 중단 시 모든 로그인·조회가 실패합니다.
2. http 주소로 열었는지(file://에서는 일부 기능 제약).
3. 우상단 상태 배지가 "DB 연결됨"인지, 콘솔에 오류가 없는지.

## 3. Zalo 로그인 (커스텀 연동 — 백엔드 구축 완료)

슈퍼베이스는 Zalo를 기본 OAuth 공급자로 제공하지 않아 Edge Function으로 커스텀 플로우를 구현했습니다. 백엔드는 이미 배포돼 있고, 아래 세 가지만 채우면 실제로 동작합니다.

이미 구축된 것(추가 작업 불필요)
- `oauth_state` 테이블: PKCE code_verifier를 state 단위로 임시 저장.
- Edge Function `zalo-login`: PKCE 생성 → 상태 저장 → Zalo 인증 URL로 리다이렉트.
- Edge Function `zalo-callback`: code→토큰 교환 → 사용자 조회 → `zalo.<id>@massa.app` 사용자 생성/획득 → 매직링크로 세션 발급 후 앱으로 복귀.
- 앱의 "Zalo로 계속" 버튼: `<SUPABASE_URL>/functions/v1/zalo-login?redirect_to=<현재앱주소>` 로 연결됨.

동작시키려면 채워야 할 3가지
1. Zalo 앱 발급: developers.zalo.me에서 앱 생성 → App ID / App Secret 발급.
   - OAuth Redirect URI(콜백)에 정확히 이 주소 등록: `https://jfnpgjcesywsxxbwlpii.supabase.co/functions/v1/zalo-callback`
2. Edge Function 시크릿 등록: Supabase 대시보드 → Edge Functions → Secrets 에서
   - `ZALO_APP_ID` = 발급받은 App ID
   - `ZALO_APP_SECRET` = 발급받은 App Secret
   - `APP_URL` = 앱이 호스팅된 https 주소(예: `https://massa.example.com/마싸_고객앱_와이어프레임.html`) — redirect_to가 없을 때의 기본 복귀 주소.
3. 호스팅: 앱을 http(s) 주소로 열어야 합니다(로컬 `file://`은 Zalo 리다이렉트 불가). 로컬 테스트는 `python -m http.server 8000` 후 `http://localhost:8000/...`.
   - 또한 Supabase → Authentication → URL Configuration의 Redirect URLs 에 그 http(s) 주소를 추가해야 매직링크 복귀가 허용됩니다.

참고: Zalo는 이메일을 항상 주지 않으므로 Zalo 고유 ID로 사용자를 식별합니다(`zalo.<id>@massa.app`). App Secret은 서버(Edge Function 시크릿)에만 두며 브라우저에는 노출되지 않습니다.

## 4. 리다이렉트 허용 목록(공통)

슈퍼베이스 → Authentication → URL Configuration에서:
- Site URL: 배포 주소(예: `https://massa.example.com`)
- Redirect URLs: 테스트용 주소도 추가(예: `http://localhost:8000`)

와이어프레임 코드는 `redirectTo: location.href`로 현재 페이지로 되돌아오게 되어 있으므로, 현재 열려 있는 주소가 위 Redirect URLs에 포함돼야 합니다.

## 4. 확인

1. 프로토타입을 http(s) 주소로 열기.
2. 계정 → 로그인 → "Google로 계속" / "카카오로 계속" 클릭.
3. 공급자 로그인 후 앱으로 복귀하면 세션 생성. profiles 행은 가입 트리거로 자동 생성됩니다.

문제가 생기면 슈퍼베이스 Authentication → Logs에서 오류를 확인하세요. "provider is not enabled"는 공급자 미활성, "redirect_uri mismatch"는 콜백 URL 불일치입니다.
